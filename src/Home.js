import React from "react";
import TodoList from "./components/TodoList";

const Home = () => {
    return (
        <section className="container">
            <TodoList/>
        </section>
    )
}

export default Home;
